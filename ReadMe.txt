The bash version can be used on any OS. 
	But it needs to be given permissions using "chmod +x script.sh"

Powershell is for windows use
	Give permissions by entering powershell as admin and run "Set-ExecutionPolicy RemoteSigned"

	Change template path if using new one
